# 🎨 透明度统一配置中心

## ⚡ 快速修改透明度（只需修改这一个地方！）

**文件位置：`src/styles/global.css`**（文件顶部第 1-20 行）

```css
:root {
  /* ========================================
     🎨 统一透明度配置中心
     所有透明度都在这里修改！修改后全站生效！
     ======================================== */
  
  /* 主要区域透明度 */
  --opacity-main: 0.6;           /* 主卡片/按钮背景 */
  --opacity-border: 0.3;         /* 边框透明度 */
  --opacity-hover: 0.7;          /* 悬停时透明度（仅文章用） */
  
  /* 次要区域透明度 */
  --opacity-secondary: 0.15;     /* 次要卡片背景 */
  --opacity-stat: 0.08;          /* 统计项背景 */
  --opacity-subtle: 0.05;        /* 装饰性元素 */
  
  /* 特殊区域透明度 */
  --opacity-fortune: 0.12;       /* 运势卡片 */
  --opacity-header: 0.6;         /* 页面标题 */
  --opacity-icon-bg: 0.2;        /* 图标背景 */
}
```

## 📊 透明度对照表

| 变量名 | 默认值 | 影响区域 | 建议范围 |
|--------|--------|----------|----------|
| `--opacity-main` | 0.6 | 主卡片、按钮、侧边栏 | 0.3 - 0.8 |
| `--opacity-border` | 0.3 | 所有边框 | 0.2 - 0.5 |
| `--opacity-hover` | 0.7 | 文章悬停效果 | 0.6 - 0.9 |
| `--opacity-secondary` | 0.15 | 页面标题区域 | 0.1 - 0.3 |
| `--opacity-stat` | 0.08 | 统计数据背景 | 0.05 - 0.15 |
| `--opacity-subtle` | 0.05 | 运势、推荐文本 | 0.03 - 0.1 |
| `--opacity-fortune` | 0.12 | 运势卡片、捐赠卡片 | 0.08 - 0.2 |
| `--opacity-header` | 0.6 | 热门/推荐页标题 | 0.3 - 0.8 |
| `--opacity-icon-bg` | 0.2 | 图标背景、登录按钮 | 0.1 - 0.4 |

## 🎯 常用修改场景

### 想让整个网站更透明？
```css
--opacity-main: 0.3;           /* 从 0.6 改成 0.3 */
--opacity-secondary: 0.08;     /* 从 0.15 改成 0.08 */
```

### 想让整个网站更不透明（更明显）？
```css
--opacity-main: 0.8;           /* 从 0.6 改成 0.8 */
--opacity-secondary: 0.3;      /* 从 0.15 改成 0.3 */
```

### 只想改侧边栏透明度？
```css
--opacity-stat: 0.15;          /* 统计项更明显 */
--opacity-subtle: 0.1;         /* 推荐文本更明显 */
```

## ✅ 已完成的优化

### 1. 透明度统一管理
- ✅ 所有透明度集中在一个地方配置
- ✅ 修改一次，全站生效
- ✅ 自动适配亮色/暗色主题

### 2. 悬停动效已关闭（文章除外）
- ❌ 卡片悬停上浮动画
- ❌ 按钮悬停上浮动画
- ❌ 侧边栏统计项悬停动画
- ❌ 导航栏登录按钮悬停动画
- ❌ 支持页面所有悬停动画
- ❌ 热门/推荐页标题图标动画
- ✅ **文章卡片保留悬停效果**（用户要求保留）

### 3. 已关闭的静态动效
- ❌ 页面进入动画（fadeIn, slideIn）
- ❌ 头像浮动动画
- ❌ 图标脉冲/发光动画
- ❌ 运势卡片淡入动画

### 4. 已关闭的性能杀手
- ❌ `backdrop-filter: blur(10px)` - 模糊效果
- ❌ `-webkit-backdrop-filter: blur(10px)`

## 🔍 如何验证修改生效？

1. 修改 `src/styles/global.css` 中的透明度值
2. 保存文件（`Ctrl + S`）
3. 浏览器自动刷新
4. 查看效果

**提示：** 建议从大到小调整，例如先改 `0.6 → 0.8` 看效果，再微调到 `0.7`

## 🌓 亮色/暗色主题说明

透明度变量会自动应用到两个主题：
- **亮色主题**：白色 + 配置的透明度
- **暗色主题**：黑色 + 配置的透明度

两个主题使用相同的透明度值，只是颜色不同。

## 💡 专业提示

1. **主卡片透明度**（`--opacity-main`）影响最大，优先调整
2. **边框透明度**（`--opacity-border`）建议比主透明度低 0.2-0.3
3. **装饰性元素**（`--opacity-subtle`）建议保持很低（0.05-0.1）
4. 修改后立即看效果，慢慢微调到满意为止

---

📝 **记住：只需修改 `src/styles/global.css` 顶部的变量，其他文件都会自动使用这些值！**
